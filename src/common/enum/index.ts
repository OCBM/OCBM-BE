export enum Role {
  SUPERADMIN = 'SUPERADMIN',
  ADMIN = 'ADMIN',
  USER = 'USER',
}
export enum PrismaValidation {
  ALREADY_EXITS = 'P2002',
}

export enum GROUP_PERMISSIONS {
  READ = 'READ',
  WRITE = 'WRITE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}
